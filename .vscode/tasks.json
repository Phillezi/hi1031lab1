{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Run Development server",
      "type": "shell",
      "command": "docker",
      "args": ["compose", "--file=${workspaceFolder}/dev.docker-compose.yml", "up"],
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "problemMatcher": [],
      "detail": "Run Tomcat, postgres and CDN using docker compose"
    }
  ]
}
